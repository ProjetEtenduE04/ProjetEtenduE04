@model IEnumerable<Clinique2000_Core.Models.Consultation>

@* <div class="container ps-0"> *@
    @* <h2>Plages Horaires</h2> *@
 @*    <table class="table mb-0 pb-0">
        <thead>
            <tr> *@
               @*  <th>Data</th> *@
      @*       </tr>
        </thead>
        <tbody>
            @foreach (var consultation in Model)
            { *@
              @*   <tr id="tableinterne" class="pb-0 mb-0">
                    <td class="ps-0 mb-0"> *@
                        @* <div>@consultation.ConsultationID</div> *@
                    @*     <div>Medecin:</div>
                        <div>Patient:</div> *@
              @*           <div>Salle:</div> *@
            @*         </td>
                <td>
                        <input type="hidden" name="id" value="@consultation.ConsultationID" />
                        <input type="hidden" name="id" value="@consultation.PatientID" />
                    <form method="post"> <button type="submit" class="btn btn-success" asp-area="Cliniques" asp-controller="ListeAttente" asp-action="ReserverConsultation">Reserver</button>
                        </form>
                </td> *@

                 @*    <td>
                        <a asp-controller="ListeAttente" asp-action="ModifierPlageHoraire" asp-route-id="@consultation.ConsultationID" class="btn btn-primary">Modifier</a>
                        <a asp-controller="ListeAttente" asp-action="SupprimerPlageHoraire" asp-route-id="@consultation.ConsultationID" class="btn btn-danger">Supprimer</a>
                    </td> *@
   @*              </tr>
            }
        </tbody>
    </table>
</div> *@


<div class="container ps-0">
    <table class="table mb-0 pb-0">
        <thead>
            <tr>
                <!-- En-têtes de colonnes si nécessaire -->
            </tr>
        </thead>
        <tbody>
            @foreach (var consultation in Model)
            {
                <tr id="tableinterne" class="pb-0 mb-0">
                    <td class="ps-0 mb-0">
                        <!-- Affichez les détails de la consultation si nécessaire -->
                        <div>Medecin: </div>
                        <div>Patient: </div>
                    </td>
                    <td>
                        <!-- Utilisez un formulaire pour chaque bouton "Réserver" -->
                        <form method="post" asp-area="Cliniques" asp-controller="ListeAttente" asp-action="ReserverConsultation">
                            <input type="hidden" name="id" value="@consultation.ConsultationID" />
                            <input type="hidden" name="patientId" value="@consultation.PatientID" />
                            <button type="submit" class="btn btn-success">Réserver</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>